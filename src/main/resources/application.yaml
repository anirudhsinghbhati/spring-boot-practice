spring:
  application:
    name: demo

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/student_db
    username: root
    password: root
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
